# DFIR_seb3684_CSEC464
CSEC464 DFIR Toolkit
Scott Brink

## Run.sh needs `sshpass` installed.
  ```
  sudo apt-get install sshpass
  ```
  ```
  sudo yum install sshpass
  ```
  ```
  sudo dnf install sshpass
  ```
  ```
  sudo pacman -S sshpass
  ```
